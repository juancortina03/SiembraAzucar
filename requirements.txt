pandas
numpy
scipy
scikit-learn
plotly
dash
requests
beautifulsoup4
lxml
openpyxl
pdfplumber
fredapi
python-dotenv
gunicorn
flask
pmdarima
xgboost
joblib
matplotlib
statsmodels
# playwright is optional — only needed to bypass Cloudflare on CONADESUCA
# scrapers. Render's default Python buildpack can't install Chromium's
# system libs without root, so we skip it. daily_update.py detects the
# absence and cleanly skips the two CONADESUCA steps.
# To enable Chromium scraping locally:  pip install playwright && playwright install chromium
